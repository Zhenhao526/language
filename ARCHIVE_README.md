# Language research archive (2026-09-17)

本归档保存语言涌现研究的源代码、研究方案、进展账本、文献索引、主要报告、轻量结果、独立审计收据和当前控制图表。当前轮为 `research_program/receiver_representation_study`。

仓库只提交可复现所需的代码和可阅读记录。逐运行 `seed_*` 目录、checkpoint 数组、训练 JSONL、模型权重、原始视觉数据、虚拟环境、文献 PDF，以及旧实验中未被报告/审计索引引用的生成细节未直接提交。省略类别、文件数量、总大小和大文件路径在 `OMISSION_SUMMARY.json` 中记录。

远端推送核验后，本地可以删除这些过程数据；当前轮源代码、方案、汇总、审计和图表继续保留。

本地清理的远端提交、保留路径和释放空间见 `LOCAL_CLEANUP_RECEIPT.json`。
