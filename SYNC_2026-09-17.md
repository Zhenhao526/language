# GitHub sync note — 2026-09-17

本次同步把当前研究路线和可复现的紧凑结果写入 `Zhenhao526/language`。核心报告是 [研究报告_共同符号形成机制_2026-09-17.md](研究报告_共同符号形成机制_2026-09-17.md)。

## 保留内容

- `research_program/tabular_signaling_study/`：无语言先验的 tabular sender/worker 正对照；保留源代码、冻结方案、144-run 紧凑轨迹、汇总和独立审计。
- `research_program/population_signaling_study/`：fixed/rotating partner、hidden/visible sender identity、scarce/abundant capacity、live/silent 的 128-run 正式矩阵；保留源代码、冻结方案、每个 run 的六个训练检查点采样、参数和日志 SHA-256、training-support/held-out 终点指标、codebook readout 与独立审计。
- `研究报告_共同符号形成机制_2026-09-17.md`：把 tabular 正对照和 population-level common-code 结果放入同一机制框架，并明确当前仍不是人类语言起源的完整模型。
- 三份 population 失效收据：记录地点观测不匹配、随机流未配对、visible 初始化混淆；这些运行不进入任何统计。

## 结果冻结

- population 正式矩阵：8 seeds × 16 conditions = 128 runs，3000 updates/run。
- population prepared SHA-256：`e05eae8c46007867151e6e2bb0cf5ea529a405ee46f181d95979e351e2d65bb0`。
- population plan SHA-256：`f49fb9a816f13318f502c878bb4d5e4736b6a76e1ee6cffdb0adb80565fd7d5d`。
- 独立审计：128 runs、384,000 training-log rows、128 terminal checkpoints，`max_abs_replay_error=0`，960,000 live/silent trajectory pairs。
- rotating-hidden＋abundant：held-out natural `0.800`，from-scratch silent `0.299`，paired live−silent `+50.05 pp`，sender token agreement `1.000`，semantic success `1.000`。
- rotating-visible＋abundant：paired live−silent `+45.60 pp`，sender token agreement `0.516`，semantic success `1.000`。

## 本地清理

远端首个同步提交验证通过后，本地删除了 12,239 个过程文件、约 3.688 GB；清单和远端提交标识见 [LOCAL_CLEANUP_RECEIPT_2026-09-17.json](LOCAL_CLEANUP_RECEIPT_2026-09-17.json)。

## 大文件策略

远端保留足以复核结果的源快照、冻结 JSON、紧凑轨迹、终点指标、哈希和审计收据；不上传每个 run 的完整 `training.jsonl`、`checkpoint_*.npz` 和 329 MB 的合并 `execution/results.json`。这些原始过程文件在远端提交验证后从本地删除，以释放磁盘空间。完整运行由 compact 结果中的固定检查点采样、日志/参数哈希和审计结果标识；需要时可按冻结方案重新生成。
