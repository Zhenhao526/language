# GitHub sync note — 2026-09-17

本次同步把当前研究路线和可复现的紧凑结果写入 `Zhenhao526/language`。核心报告是 [研究报告_共同符号形成机制_2026-09-17.md](研究报告_共同符号形成机制_2026-09-17.md)。

## 保留内容

- `research_program/tabular_signaling_study/`：无语言先验的 tabular sender/worker 正对照；保留源代码、冻结方案、144-run 紧凑轨迹、汇总和独立审计。
- `research_program/population_signaling_study/`：fixed/rotating partner、hidden/visible sender identity、scarce/abundant capacity、live/silent 的 128-run 正式矩阵；保留源代码、冻结方案、每个 run 的六个训练检查点采样、参数和日志 SHA-256、training-support/held-out 终点指标、codebook readout 与独立审计。
- `研究报告_共同符号形成机制_2026-09-17.md`：把 tabular 正对照和 population-level common-code 结果放入同一机制框架，并明确当前仍不是人类语言起源的完整模型。
- 三份 population 失效收据：记录地点观测不匹配、随机流未配对、visible 初始化混淆；这些运行不进入任何统计。

## 结果冻结

- population 正式矩阵：8 seeds × 16 conditions = 128 runs，3000 updates/run。
- population prepared SHA-256：`e05eae8c46007867151e6e2bb0cf5ea529a405ee46f181d95979e351e2d65bb0`。
- population plan SHA-256：`f49fb9a816f13318f502c878bb4d5e4736b6a76e1ee6cffdb0adb80565fd7d5d`。
- 独立审计：128 runs、384,000 training-log rows、128 terminal checkpoints，`max_abs_replay_error=0`，960,000 live/silent trajectory pairs。
- rotating-hidden＋abundant：held-out natural `0.800`，from-scratch silent `0.299`，paired live−silent `+50.05 pp`，sender token agreement `1.000`，semantic success `1.000`。
- rotating-visible＋abundant：paired live−silent `+45.60 pp`，sender token agreement `0.516`，semantic success `1.000`。

## 本地清理

远端首个同步提交验证通过后，本地删除了 12,239 个过程文件、约 3.688 GB；清单和远端提交标识见 [LOCAL_CLEANUP_RECEIPT_2026-09-17.json](LOCAL_CLEANUP_RECEIPT_2026-09-17.json)。

## 大文件策略

远端保留足以复核结果的源快照、冻结 JSON、紧凑轨迹、终点指标、哈希和审计收据；不上传每个 run 的完整 `training.jsonl`、`checkpoint_*.npz` 和 329 MB 的合并 `execution/results.json`。这些原始过程文件在远端提交验证后从本地删除，以释放磁盘空间。完整运行由 compact 结果中的固定检查点采样、日志/参数哈希和审计结果标识；需要时可按冻结方案重新生成。

## 双因子组合性正式批次

- `research_program/compositional_signaling_study/`：比较等容量的一个四值 token（`mono4`）与两个二值 token（`dual2`），并测试整体消息置换、跨 worker 一致性与 held-out 槽位重组。
- 正式矩阵为 8 seeds × 10 conditions = 80 runs，每个 run 3000 updates；独立审计通过 240,000 条训练日志、80 个终点 checkpoint、120,000 对 live/silent 轨迹，`max_abs_replay_error=0`。
- 轮换隐藏 factorized 条件的 held-out natural/silent 为 `0.499/0.249`，paired live−silent `+0.250`（95% t 区间 `[+0.186,+0.314]`）；dual2 槽位重组−natural 为 `−0.117`（`[−0.224,−0.011]`）。共享整体码本没有自动产生可重组词法。
- 伙伴可见时完整序列一致率为 `0.484`，说明伙伴专用协议可以和总体通信收益并存。
- GitHub 只保留冻结方案、源码、稀疏轨迹样本、终点指标、哈希和审计；完整 `training.jsonl`、checkpoint 与合并结果已省略。

## 新成员加入与协议传递正式批次

- `research_program/generation_transmission_study/`：先训练 rotating-hidden parent population，再分别替换 worker 0 或 sender；child 只通过 token、行动后果和奖励适应。
- 8 个 seed 共享 8 次 parent 训练，6 个 child channel 条件（worker/sender × live/silent/scrambled）共 48 个 child run，每个 run 3000 updates。
- held-out 新 worker 的 live−silent 为 `+0.598`（95% t `[+0.540,+0.656]`），新 sender 为 `+0.484`（`[+0.329,+0.639]`）；live−scrambled 分别为 `+0.516` 和 `+0.453`。
- 独立审计通过 8 parent、48 child、168,000 training-log rows、56 terminal checkpoints、48,000 paired child trajectory rows，`max_abs_replay_error=0`。
- GitHub 只保留冻结方案、源码、稀疏轨迹、终点指标、学习曲线、哈希和审计；原始日志、checkpoint 与合并结果已省略。

## 行动依赖消息时机与组合性正式批次

- `research_program/action_dependent_signaling_study/` 比较相同 dual2 消息容量下的 `simultaneous`（完整序列在第一个行动前到达）与 `staged`（两个子任务前各到达一个槽位），使用 rotating-hidden、factorized/entangled、live/silent，共 32 seeds × 8 conditions = 256 runs，每个 run 3000 updates。
- 可组合平衡预注册为 held-out natural ≥ 0.60 且 `|recombined−natural|≤0.02`：factorized 为 simultaneous 3/32、staged 9/32；entangled 为 0/32、10/32。staged−simultaneous natural 的配对差为 factorized +2.2 pp（95% t CI [−1.6,+6.0]），entangled +5.2 pp（[+1.6,+8.8]）；McNemar 精确双侧 p 分别为 0.0703 和 0.0020。
- staged 的平均 `recombined−natural` 仍为 factorized −13.5 pp、entangled −11.1 pp，说明行动依赖提高了进入高回报且重组无损平衡的概率，却没有普遍产生槽位可重组词法。
- 独立审计通过 256 runs、768,000 条 training-log rows、256 个终点 checkpoint、384,000 对 live/silent trajectory rows，`max_abs_replay_error=0`。
- GitHub 保留源代码、冻结方案、完整审计、紧凑终点和稀疏轨迹；逐 run 的完整 `training.jsonl`、checkpoint arrays 和合并 `results.json` 已省略。

## staged dual2 组合协议的跨主体传递

- `research_program/generation_compositional_transmission_study/` 固定行动依赖实验的 staged、dual2、factorized parent，分别替换 worker 0 或 hidden sender；32 个 parent × 2 roles × live/silent/partner-permuted = 192 child runs，每个 3000 updates。
- 先前 32 个 parent 中 9 个满足 natural ≥ 0.60 且 `|recombined−natural|≤0.02`。这 9 个 parent 的新 worker 和新 sender 均 9/9 恢复组合平衡；其余 23 个 parent 两方向均 0/23。
- 独立审计通过 192 runs、576,000 条 training-log rows、192 个终点 checkpoint、384,000 行配对轨迹，`max_abs_replay_error=0`。GitHub 只保留紧凑结果和哈希，parent/child 原始 execution 省略。

## 未见目标组合留出正式批次

- `research_program/heldout_composition_study/` 在同一 staged dual2 parent sender 下替换 worker 0；`full/leave_one_out × live/silent` 共 32 seeds × 4 conditions = 128 child runs，每个 3000 updates。leave-one-out 支持删除一个目标组合，留出组合按 seed 均衡。
- full live 的留出组合均值为 0.546，leave-one-out 为 0.254，配对差 −29.3 pp（95% t CI [−34.4,−24.1]）；leave-one-out live−silent 为 +31.9 pp（[+26.8,+37.1]），但 32 个 seed 均未达到 0.60 的零样本阈值。
- 独立审计通过 128 runs、384,000 条 training-log rows、512 个 checkpoint、192,000 行 live/silent 配对轨迹，`max_abs_replay_error=0`。留出结果支持“当前接收者仍依赖见过的联合消息状态”的负边界，不等于对所有架构的组合泛化定理。
